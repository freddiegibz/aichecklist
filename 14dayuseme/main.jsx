import React from "react";
import { createRoot } from "react-dom/client";
import MilestoneTracker from "./14daycourse.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MilestoneTracker />
  </React.StrictMode>
);
